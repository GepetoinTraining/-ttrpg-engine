'use client'

import Tactical from '@/components/design/surfaces/Tactical'

// /dm/tactical — wraps the existing Tactical surface inside the (real) layout.

export default function TacticalPage() {
  return <Tactical />
}
