'use client'

import Studies from '@/components/design/surfaces/Studies'

// /dm/studies — wraps the existing Studies surface inside the (real) layout.

export default function StudiesPage() {
  return <Studies />
}
