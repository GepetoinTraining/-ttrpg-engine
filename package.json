{
  "name": "ttrpg-engine",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.4",
  "workspaces": [
    "bend",
    "fend"
  ],
  "scripts": {
    "start": "bun run launcher.ts",
    "dev": "bun run launcher.ts",
    "dev:bend": "cd bend && bun run dev",
    "dev:fend": "cd fend && bun run dev",
    "build": "turbo run build",
    "build:all": "bun run build:bend && bun run build:fend",
    "build:bend": "cd bend && bun run build",
    "build:fend": "cd fend && bun run build"
  },
  "dependencies": {
    "@libsql/client": "^0.15.15",
    "@libsql/linux-x64-gnu": "^0.5.22",
    "@trpc/server": "^11.8.1",
    "@ttrpg/bend": "workspace:*",
    "@vercel/node": "^5.5.16",
    "ws": "^8.18.3",
    "zod": "^4.2.1"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.9.3"
  }
}
